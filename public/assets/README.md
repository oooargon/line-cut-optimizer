# Assets Directory

Place your logo image here as `header.png`.

The application will automatically load the logo from `/assets/header.png`.

If the image is not found, the logo area will be hidden automatically.

